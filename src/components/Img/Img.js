import { styled } from '@mui/material/styles'

/**Component to use prop sx in an img tag */
const Img = styled('img')({
    display: 'block',
    maxWidth: '100%',
    maxHeight: '100%',
})

export default Img